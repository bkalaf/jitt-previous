// import { MRT_RowData } from 'material-react-table';
// import { getProperty } from '../../common/object';

// export function createLabelPropertyCell<T extends MRT_RowData>(labelProperty: keyof T & string) {
//     return function LabelPropertyCell({ data, className }: { data: T; className?: string }) {
//         const value = getProperty(labelProperty as any, data)?.toString();
//         return <span className={className}>{value}</span>;
//     };
// }
