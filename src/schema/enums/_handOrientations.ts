// export const _handOrientations = {
//     rh: 'right-handed',
//     lh: 'left-handed'
// };

// const aliasHandOrientation = {
//     'right-handed': 'rh',
//     'left-handed': 'lh',
//     'right handed': 'rh',
//     'left handed': 'lh'
// };

// const handOrientationMap = Object.fromEntries(Object.entries(_handOrientations).map(([k, v]) => [k, { key: k, text: v }]));

// export const handOrientations = {
//     ...handOrientationMap,
//     ...Object.fromEntries(Object.entries(aliasHandOrientation).map(([k, v]) => [k, handOrientationMap[v]]))
// };

// console.log(handOrientations);
