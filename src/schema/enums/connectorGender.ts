export const connectorGenders = { m: { text: 'Male', key: 'm' }, f: { text: 'Female', key: 'f' } };

// console.log(Object.fromEntries(Object.entries(connectorGenders).map(([k, v]) => [k, { text: v, key: k }])));