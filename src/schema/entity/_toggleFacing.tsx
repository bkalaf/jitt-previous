// import { IFacing } from '../../types';
// import { runTransaction } from '../../util/runTransaction';
// import Realm from 'realm';

// export function toggleFacing<TKey extends keyof IFacing>(realm: Realm, facing: IFacing, key: TKey, value: IFacing[TKey]) {
//     const func = () => (facing[key] = value);
//     runTransaction(realm, func);
//     return facing;
// }
