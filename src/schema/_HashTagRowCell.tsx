// import { IHashTag } from '../types';
// import { $className } from '../util/$className';

// export function HashTagRowCell(props: { data: IHashTag; className?: string }) {
//     const { className, data } = $className(props, {}, 'text-base', 'flex text-sm justify-start w-full');
//     return <span className={className}>{data.name}</span>;
// }
