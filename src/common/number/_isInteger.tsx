// import { truncate } from './truncate';

// export function isInteger(value?: string | number) {
//     if (value == null) return false;
//     const $value = typeof value === 'string' ? parseFloat(value) : value;
//     return (
//         $value == null ? false
//         : truncate($value)?.toString().includes('.') ? false
//         : true
//     );
// }
