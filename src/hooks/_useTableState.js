// import { useMemo } from 'react';
// import { defaultState } from './defaultState';
// export function useTableState<T extends keyof IConfig>(config: Record<string, IConfig>, collection: string, key: T) {
//     return useMemo(() => (Object.hasOwn(config, collection) && Object.hasOwn(config[collection], key) ? config[collection][key] : defaultState[key]), [collection, config, key]);
// }
//# sourceMappingURL=_useTableState.js.map