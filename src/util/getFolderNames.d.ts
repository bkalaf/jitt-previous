import { ISku } from '../types';
export declare function getFolderNames(sku: ISku): [brandFolder: string, productFolder: string, skuFolder: string];
export declare function getRemBgName(filename: string, suffix: string, ext: string): string;
